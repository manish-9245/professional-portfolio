{
  "name": "professional-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate:blogs && npm run generate:projects",
    "generate:blogs": "node scripts/generate-blog-share-pages.mjs",
    "generate:projects": "node scripts/generate-project-pages.mjs"
  },
  "devDependencies": {
    "highlight.js": "^11.11.1",
    "jsdom": "^29.1.1",
    "marked": "^18.0.4"
  }
}
